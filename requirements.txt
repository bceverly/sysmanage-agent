# Capped below 16.0 because websockets 16.x dropped Python 3.9 wheels.
# OL9 / RHEL 9 / CentOS Stream 9 / Amazon Linux 2023 ship cp39 as the
# default python3, and the rpm's vendor dir relies on per-Python wheel
# downloads; a >=15.0.1 unbounded constraint resolved to 16.0, which has
# no cp39 wheel, leaving cp39 targets without a websockets install and
# crashing the agent with "No matching distribution". Bump the upper
# bound only when cp39 is dropped from the supported Python matrix.
websockets>=15.0.1,<16
PyYAML>=6.0.2
# aiohttp 3.14.x (carrying the PYSEC-2026-2104..2113 fix) requires Python >=3.10,
# so the agent's 3.9 leg (RHEL9/OL9 cp39) can't install it and no 3.13.x backport
# of the fix exists.  On 3.10+ take the fixed 3.14.1; on 3.9 stay on the newest
# 3.9-compatible line (3.13.x) — an accepted, unavoidable gap until aiohttp
# backports or cp39 is dropped from the support matrix.
aiohttp>=3.14.1; python_version >= '3.10'
aiohttp>=3.13.5,<3.14; python_version < '3.10'
cryptography>=48.0.1  # 45.0.7-47.x buffer overflow + DNS name-constraint CVEs; 48.0.1 fixes GHSA-537c-gmf6-5ccf (48.0.0 affected). Supports cp39.
psutil>=5.9.0
bcrypt>=4.0.0
aiofiles>=24.1.0
# defusedxml is used by package_collector_windows when parsing the
# Chocolatey API XML response — even though Chocolatey is a trusted
# endpoint, parsing untrusted XML via stdlib ``xml.etree`` is the
# pattern semgrep ``use-defused-xml`` (correctly) flags.  defusedxml
# rejects external-entity references + billion-laughs payloads.
defusedxml>=0.7.1
# python_multipart was briefly pinned here as defense-in-depth against
# the 0.0.20 DoS + form-parser-bypass CVE chain that affected the
# server.  Removed because:
#   1. ``pip download --python-version 3.9.21`` evaluates env markers
#      against the CI HOST's python (3.12), not the target — so a
#      ``; python_version >= '3.10'`` gate doesn't actually skip the
#      pin in the Python-3.9 wheel-download passes that the RPM
#      builder uses.  pip then can't find a 0.0.27+ wheel for cp39
#      (no such release exists) and the build fails.
#   2. The agent has no untrusted-form-parser surface, so the CVE
#      class doesn't reach it even if 0.0.20 lands transitively.
# If a future code change starts parsing multipart input on the
# agent, add the pin back AND split requirements per Python version.

# Database dependencies
SQLAlchemy>=2.0.43
alembic>=1.16.5
# Transitive but SHIPPED at runtime, so pinned to their patched releases:
#   Mako  — pulled by alembic (migration templates).  PYSEC-2026-2617 fixed in 1.3.12.
#   idna  — pulled by aiohttp (via yarl) for hostname handling.  PYSEC-2026-215
#           fixed in 3.15.
Mako>=1.3.12
idna>=3.15

# Development dependencies for linting and testing
babel>=2.12.0  # pybabel CLI for i18n-validate
# black is dev-only (``make lint``).  CVE-2026-32274 (arbitrary file writes
# via crafted ``--python-cell-magics`` cache filename) is fixed in 26.3.1+,
# which requires Python >=3.10.  Older patched releases for 3.9 don't
# exist — so we install black ONLY on Py 3.10+, and skip it on Py 3.9.
# The 3.9 CI jobs run ``pip install`` + ``pytest`` but not ``make lint``;
# lint runs on 3.10+ only.
black>=26.3.1; python_version >= '3.10'
pylint>=3.0.0
isort>=5.12.0
bandit>=1.8.6
safety>=3.6.1,<4.0.0
# semgrep 1.137+ requires Python >=3.10; agent CI matrix includes 3.9.
# 1.162.0 relaxed tomli~=2.0.1 → ~=2.4.0 (compatible with pip-audit) — used
# on >=3.10.  On 3.9, 1.136.0 is the last release; the older tomli pin
# is fine because the agent doesn't ship pip-audit (see Makefile).
semgrep>=1.162.0; python_version >= '3.10'
semgrep>=1.95.0,<1.137; python_version < '3.10'
typer>=0.9.0,<0.24.0
# pytest 9.x (which carries the PYSEC tmp-dir advisory fix) requires Python
# >=3.10, so the agent's 3.9 CI leg can't install it.  On 3.10+ we take the
# fixed 9.0.3+; on 3.9 we stay on the newest 3.9-compatible pytest (8.4.x) and
# accept that dev-tool-only medium advisory — pytest is never shipped with the
# agent, and no 3.9-compatible release carries the fix.
pytest>=9.0.3; python_version >= '3.10'
pytest>=8.4.2,<9; python_version < '3.10'
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
pytest-xdist>=3.0.0
coverage>=7.0.0

# --- Transitive security pins for DEV / CI tooling only (safety, semgrep,
#     selenium, requests) — NOT shipped with the agent runtime.  Pinned to their
#     patched releases so the dependency scanners come back clean. ---
# urllib3 2.7 dropped Python 3.9, so the 3.9 leg can't take >=2.7.0 (its ceiling
# is the 2.6.x line).  Split like aiohttp/pytest: latest fix on 3.10+, highest
# 3.9-compatible secure release on 3.9.  PYSEC-2026-141/142 (pulled by
# requests/semgrep/selenium).  Scanners run on the 3.10+ leg, which takes >=2.7.0.
urllib3>=2.7.0; python_version >= '3.10'
urllib3>=2.5.0,<2.7; python_version < '3.10'
ujson>=5.13.0  # PYSEC-2026-2293/2294 (pulled by semgrep)
Authlib>=1.7.2  # advisory affecting 1.7.0 (pulled by safety)
# python_multipart: still intentionally UNPINNED despite PYSEC-2026-3036..3040.
# It reaches the agent only through dev tooling (no untrusted-form-parser
# surface at runtime), and pinning 0.0.27+ breaks the Python-3.9 wheel-download
# pass the RPM builder uses (see the note above the Database section). Revisit
# if/when cp39 is dropped from the support matrix.

# Note: On OpenBSD 7.7, coverage.py C tracer requires gcc and py3-cffi
# Install with: doas pkg_add gcc py3-cffi
# The install-dev target will automatically handle C tracer setup
