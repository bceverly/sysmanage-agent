# Production runtime dependencies only
# This file is used by the installer to create production packages
# For development dependencies, see requirements.txt

# Core communication
websockets>=15.0.1
PyYAML>=6.0.2
aiohttp>=3.12.15
cryptography>=45.0.7

# Database dependencies
SQLAlchemy>=2.0.43
alembic>=1.16.5

# Windows service support (Windows only, gracefully ignored on other platforms)
pywin32>=306; sys_platform == 'win32'
