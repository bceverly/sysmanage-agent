# Core dependencies for SysManage Agent on OpenBSD
# This file avoids cryptography-dependent packages that cause build issues on OpenBSD

websockets>=13.0
PyYAML>=6.0.2
aiohttp>=3.12.0

# Basic testing (without coverage that depends on cryptography)
pytest>=7.0.0
pytest-asyncio>=0.21.0